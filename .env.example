# Public site URL for metadata (omit trailing slash). Falls back to VERCEL_URL then localhost.
# NEXT_PUBLIC_SITE_URL=https://app.example.com

# Hirevine API (hirevine-v2-be). Default matches local API dev server.
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
